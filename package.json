{
  "name": "forma-deploy",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build && cp -r build/* ../backend/static/"
  }
}
